// Generated prose, edited as markdown. The agent runbook is served at
// foo.gl/agent.md; llms.txt is the answer-engine summary.
export const AGENT_MD = "# Set up foogl (instructions for an agent)\n\nYou are setting up foogl, a self-hosted link shortener, on your human's own\nCloudflare account. One Worker, one D1 database, no build step; it fits in\nCloudflare's free tier. Follow the steps in order. Every step has a check.\nIf a check fails, stop and show your human the exact error; do not improvise.\n\nHumans: the README covers this same setup for people, with a one-click\nbutton: https://github.com/duskresearch/foogl#readme\n\n## Ask your human first\n\n1. A dashboard password (or offer to generate a strong one and show it once).\n2. Optional: a custom domain for the short links (for example go.example.com).\n   It must be on a Cloudflare zone in the same account; workers.dev works fine\n   without one, so this can wait.\n\nAsk nothing else. Everything below is decided for you.\n\n## Prerequisites (check, do not assume)\n\n- Node 18 or newer: `node --version`\n- Wrangler authenticated to the right account: `npx wrangler@latest whoami`\n  If it is not logged in, have your human run `npx wrangler login` themselves;\n  it opens a browser. Never handle their Cloudflare credentials yourself.\n\n## Steps\n\n1. Get the code and dependencies.\n\n       git clone https://github.com/duskresearch/foogl.git\n       cd foogl && npm install\n\n   Check: `ls migrations` lists five .sql files.\n\n2. Create the database, and wire its id into the config.\n\n       npx wrangler d1 create foogl-db\n\n   Copy the printed `database_id` into `wrangler.jsonc` under `d1_databases`,\n   replacing the placeholder id.\n   Check: `grep database_id wrangler.jsonc` shows the id you just created.\n   If `d1 create` says the name is taken, the human already has a foogl-db:\n   use `npx wrangler d1 list` to find its id instead of creating another.\n\n3. Build the tables.\n\n       npx wrangler d1 migrations apply foogl-db --remote\n\n   Check: the output lists migrations as applied. Skipping this is the cause\n   of the \"Database not initialised\" error later.\n\n4. Set the dashboard password as a secret. It must never land in a file.\n\n       npx wrangler secret put SITE_PASSWORD\n\n   Paste the password at the prompt.\n\n5. Deploy.\n\n       npx wrangler deploy\n\n   The output ends with a `*.workers.dev` URL; call it $APP.\n   Check: `curl -s -o /dev/null -w '%{http_code}' $APP/login` prints 200.\n\n6. Prove a link works end to end. Have your human sign in at $APP and create\n   one link, or do it yourself with their permission using the dashboard.\n   Check: `curl -s -o /dev/null -w '%{http_code} %{redirect_url}' $APP/<slug>`\n   prints 301 or 302 and the destination.\n\n7. Custom domain, only if the human asked for one. Use the Cloudflare\n   dashboard: the Worker -> Settings -> Domains & Routes -> Add -> Custom\n   Domain. Do NOT hand-edit a `routes` array into wrangler.jsonc: a partial\n   routes array on a later deploy can silently unbind other domains.\n   Check with the public resolver, never the local one (a premature lookup\n   through the local resolver caches the miss for up to half an hour):\n\n       dig +short go.example.com @1.1.1.1\n       curl -s -o /dev/null -w '%{http_code}' --resolve go.example.com:443:$(dig +short go.example.com @1.1.1.1 | head -1) https://go.example.com/login\n\n8. Optional: enable the HTTP API from Settings -> API in the dashboard, which\n   generates a bearer token. `POST /api/v1/links` with\n   `Authorization: Bearer <token>` then creates links from scripts.\n\n## Troubleshooting\n\n- \"Database not initialised. Run the migrations\": step 3 was skipped, or ran\n  against local instead of `--remote`.\n- Every page 500s: run `npx wrangler tail` while reproducing; the most common\n  cause is a wrong `database_id` in wrangler.jsonc.\n- \"exceeded D1's free tier daily limit\": some other D1 database on the same\n  account spent the day's shared allowance; it resets at midnight UTC.\n- Custom domain 522 or 1016: the zone is not in this account, or DNS records\n  for that hostname already existed. Fix in the Cloudflare dashboard.\n\n## Do not\n\n- Put SITE_PASSWORD (or anything secret) in wrangler.jsonc or any file.\n- Add a `routes` array to wrangler.jsonc.\n- Create a second database when foogl-db already exists.\n- Continue past a failed check.\n"
export const LLMS_TXT = "# foogl\n\n> A tiny, self-hosted link shortener you deploy to your own Cloudflare account in one click.\n\nfoogl gives you short links on your own domain with a clean dashboard and\nsimple analytics: clicks over time, countries, referrers, devices. Link\nexpiry, platform and country targeting, query passthrough, a UTM builder,\nreferrer hiding, QR codes, social previews, CSV export, and an opt-in HTTP\nAPI. One password, no accounts. It runs entirely on Cloudflare's free tier:\none Worker and one D1 database, no build step. It deliberately does less\nthan the big shorteners: no teams, no A/B tests, just the short-link loop\ndone well.\n\nSite: https://foo.gl\nSource (MIT): https://github.com/duskresearch/foogl\nBy: Dusk Research, https://duskresearch.com\n\n## For agents\n\nTo set foogl up for your human on their own Cloudflare account, fetch\nhttps://foo.gl/agent.md and follow it exactly. It contains every step, a\ncheck for each, and the two questions worth asking first.\n"
