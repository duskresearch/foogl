// Generated prose, edited as markdown. The agent runbook is served at
// foo.gl/agent.md; llms.txt is the answer-engine summary.
export const AGENT_MD = "# Set up foogl (instructions for an agent)\n\nYou are setting up foogl, a self-hosted link shortener, on your human's own\nCloudflare account. One Worker, one D1 database, no build step; it fits in\nCloudflare's free tier. Follow the steps in order. Every step has a check.\nIf a check fails, stop and show your human the exact error; do not improvise.\n(One exception: the deploy check in step 5 is allowed its retry window.)\n\nHumans: the README covers this same setup for people, with a one-click\nbutton: https://github.com/duskresearch/foogl#readme\n\n## Ask your human first\n\n1. How they want the dashboard password handled. Two options, in order of\n   preference:\n   - THEY run one command themselves in their own terminal (step 4 shows it),\n     so the password never passes through you. This is the default.\n   - You GENERATE a strong random password, set it, and show it to them once\n     at the end to store in their password manager.\n   Never ask a human to tell you a password. Anything typed into this\n   conversation ends up in a transcript.\n2. Optional: a custom domain for the short links (for example go.example.com).\n   It must be on a Cloudflare zone in the same account; workers.dev works fine\n   without one, so this can wait.\n\nAsk nothing else. Everything below is decided for you.\n\n## Prerequisites (check, do not assume)\n\n- Node 18 or newer: `node --version`\n- Wrangler authenticated to the right account: `npx wrangler whoami`\n  If it is not logged in, have your human run `npx wrangler login` themselves;\n  it opens a browser. Never handle their Cloudflare credentials yourself.\n\n## Steps\n\n1. Get the code and dependencies.\n\n       git clone https://github.com/duskresearch/foogl.git\n       cd foogl && npm install\n\n   Check: `ls migrations` lists numbered .sql files (0001 through at least 0005).\n\n2. Create the database, and wire its id into the config.\n\n       npx wrangler d1 create foogl-db\n\n   The output prints a `database_id`. The committed `wrangler.jsonc` ships\n   WITHOUT one (the one-click button injects it), so ADD the key inside the\n   `d1_databases` entry, next to `database_name`:\n\n       \"database_id\": \"<the id you just created>\"\n\n   Check: `grep -o '\"database_id\": *\"[0-9a-f-]*\"' wrangler.jsonc` prints the id.\n   If `d1 create` says the name is taken, the human already runs foogl. Ask\n   whether this is a second instance; if so, pick fresh names for BOTH the\n   worker (`name` in wrangler.jsonc) and the database, and never point a new\n   instance at an existing database.\n\n3. Build the tables.\n\n       npx wrangler d1 migrations apply foogl-db --remote\n\n   Check: the output lists the migrations as applied. Skipping this causes\n   the \"Database not initialised\" error later.\n\n4. Set the dashboard password as a secret. It must never land in a file, and\n   it should not pass through you either.\n\n   Default: hand your human this one command to run in their own terminal,\n   in this directory, and wait for them to confirm it succeeded:\n\n       npx wrangler secret put SITE_PASSWORD\n\n   (They paste the password at the prompt. Because the Worker does not exist\n   yet, wrangler asks whether to create it; they answer yes.)\n\n   Fallback, only if they chose it: generate a strong random password\n   yourself, for example `openssl rand -base64 18`, pipe it in\n   (`echo \"$GENERATED\" | npx wrangler secret put SITE_PASSWORD`, answering\n   yes to the create-worker prompt), and show it to them once at the very\n   end. Never reuse anything they have typed to you as a password.\n\n   Check: the command output says the secret was uploaded.\n\n5. Deploy.\n\n       npx wrangler deploy\n\n   The output ends with a `*.workers.dev` URL; call it $APP. A warning about\n   preview_urls can be ignored. A fresh workers.dev hostname can 404 for the\n   first ~30 seconds while DNS propagates, so retry this check for up to a\n   minute before treating it as a failure:\n\n       curl -s -o /dev/null -w '%{http_code}' $APP/login   # expect 200\n\n6. Prove a link works end to end. Default: your human signs in at $APP and\n   creates one link in the dashboard (any URL, slug `hello`), and tells you.\n   Only in the generated-password fallback may you do it yourself with curl\n   (the login field is `password`; link fields are `url` and `slug`):\n\n       curl -s -c /tmp/foogl.jar -d \"password=$GENERATED\" $APP/login\n       curl -s -b /tmp/foogl.jar -d \"url=https://example.com/hello\" -d \"slug=hello\" $APP/api/links\n\n   Check either way: `curl -s -o /dev/null -w '%{http_code} %{redirect_url}' $APP/hello`\n   prints 301 or 302 and the destination.\n\n7. Custom domain, only if the human asked for one. Use the Cloudflare\n   dashboard: the Worker -> Settings -> Domains & Routes -> Add -> Custom\n   Domain. Do NOT hand-edit a `routes` array into wrangler.jsonc: a partial\n   routes array on a later deploy silently unbinds any domain it omits.\n   Verify with the public resolver, never the local one (a premature local\n   lookup caches the miss for up to half an hour):\n\n       dig +short go.example.com @1.1.1.1\n\n8. Optional: enable the HTTP API from Settings -> API in the dashboard, which\n   generates a bearer token. `POST /api/v1/links` with\n   `Authorization: Bearer <token>` then creates links from scripts.\n\n## Troubleshooting\n\n- \"Database not initialised. Run the migrations\": step 3 was skipped, or ran\n  without `--remote`.\n- Every page 500s: run `npx wrangler tail` while reproducing; the most common\n  cause is a wrong `database_id` in wrangler.jsonc.\n- \"exceeded D1's free tier daily limit\": some other D1 database on the same\n  account spent the day's shared allowance; it resets at midnight UTC.\n- Custom domain 522 or 1016: the zone is not in this account, or DNS records\n  for that hostname already existed. Fix in the Cloudflare dashboard.\n\n## Do not\n\n- Ask the human to tell you a password, ever.\n- Put SITE_PASSWORD (or anything secret) in wrangler.jsonc or any file.\n- Add a `routes` array to wrangler.jsonc.\n- Point a new instance at an existing database.\n- Continue past a failed check (step 5's retry window excepted).\n"
export const LLMS_TXT = "# foogl\n\n> A tiny, self-hosted link shortener you deploy to your own Cloudflare account in one click.\n\nfoogl gives you short links on your own domain with a clean dashboard and\nsimple analytics: clicks over time, countries, referrers, devices. Link\nexpiry, platform and country targeting, query passthrough, a UTM builder,\nreferrer hiding, QR codes, social previews, CSV export, and an opt-in HTTP\nAPI. One password, no accounts. It runs entirely on Cloudflare's free tier:\none Worker and one D1 database, no build step. It deliberately does less\nthan the big shorteners: no teams, no A/B tests, just the short-link loop\ndone well.\n\nSite: https://foo.gl\nSource (MIT): https://github.com/duskresearch/foogl\nBy: Dusk Research, https://duskresearch.com\n\n## For agents\n\nTo set foogl up for your human on their own Cloudflare account, fetch\nhttps://foo.gl/agent.md and follow it exactly. It contains every step, a\ncheck for each, and the two questions worth asking first.\n"
